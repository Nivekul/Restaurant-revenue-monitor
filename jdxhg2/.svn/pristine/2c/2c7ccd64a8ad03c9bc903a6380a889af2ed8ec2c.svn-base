from django.conf.urls import include, url
from . import views
from . import menu

urlpatterns = [
    url(r'^$', views.index),
    url(r'^menu/create/', menu.create),
    url(r'^menu/get/', menu.get),
]
